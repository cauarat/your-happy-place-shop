# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.*
# The template is the one env file meant to be shared — it holds the names of
# the variables, never their values.
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
credentials.json
.tmp/

# Firecrawl exports, the importer's image cache and its last report. The dumps
# are large and are not the catalogue — `src/data/catalog.json` is.
data/firecrawl/

# Measurements the cover picker caches; derived from the photos, not a decision.
data/curation/.cover-scores.json
